framework:
    rate_limiter:
        admin_login:
            policy: sliding_window
            limit: 5
            interval: '15 minutes'
        contact_form:
            policy: sliding_window
            limit: 5
            interval: '15 minutes'
        estimation_form:
            policy: sliding_window
            limit: 10
            interval: '15 minutes'
        lead_form:
            policy: sliding_window
            limit: 10
            interval: '15 minutes'

when@test:
    framework:
        rate_limiter:
            admin_login:
                policy: sliding_window
                limit: 1000
                interval: '1 minute'
            contact_form:
                policy: sliding_window
                limit: 1000
                interval: '1 minute'
            estimation_form:
                policy: sliding_window
                limit: 1000
                interval: '1 minute'
            lead_form:
                policy: sliding_window
                limit: 1000
                interval: '1 minute'
